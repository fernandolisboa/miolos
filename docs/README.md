# Docs

Project documentation. Two kinds of file live here, and they follow different rules.

## Living documents (no number)

Read them for the current state of the project. They are edited in place.

| Path | What it is |
|---|---|
| `agents/issue-tracker.md` | Where issues live and how agents operate on them |
| `agents/triage-labels.md` | The five canonical triage roles → this repo's label strings |
| `agents/domain.md` | How agents must consume `CONTEXT.md` and ADRs, and the ADR status lifecycle (statuses, owner, trigger, header forms) |
| `agents/test-ids.md` | The `T-<AREA>-[S]<n>[<letter>]` convention, the per-area frontier and the burned slots |
| `pending-fernando.md` | The pending-on-Fernando ledger — every action/decision only Fernando can take, wizard-ready, maintained by every session that surfaces or discharges one |
| `adr/` | Architecture Decision Records (`0001-…`, own 4-digit sequence) |

`CONTEXT.md` (the domain glossary) lives at the repo root, not here — see `agents/domain.md`.

## Sequenced artifacts (numbered)

Handoffs, briefs, plans and specs are **snapshots of a point in time**, not living specs. Their bodies are never rewritten to stay current: a newer document supersedes an older one, and where a decision changes part of one, an **amendment table is prepended** pointing at the ADR that supersedes it. Never edit the body to match a later decision. The number tells you the order they were created and used.

```
NNN[-issue-<n>]-<type>-<slug>.md
```

- **`NNN`** — a single sequence shared across every subdirectory, so creation order stays readable no matter where a file sits. Next free number wins.
- **`issue-<n>`** — optional. Present only when the document belongs to exactly one GitHub issue. Omitted for documents written before any issue exists, and for documents that span a whole milestone. Don't force it: a handoff covering M0 is not "issue 12's handoff".
- **`<type>`** — `handoff`, `brief`, `plan`, `spec`, `research`.
- **`<slug>`** — short, kebab-case, English.

Current:

| File | Type |
|---|---|
| `handoffs/001-handoff-project-foundation.md` | Founding handoff — **the source of truth**, as amended by the table at its top |
| `design/002-brief-design-direction.md` | Visual direction brief, feeds the Claude Design exploration |
| `research/003-research-universal-rn-web.md` | Whether universal Expo Router + react-native-web fits this product |
| `research/004-research-web-native-code-sharing.md` | Two UIs sharing a core, and the universal styling systems |
| `research/005-research-miolos-name-check.md` | "Miolos" domain + INPI check (2026-07-30 snapshot); sources the Android-share figure |
| `design/006-handoff-design-winner-atelie/` | Handoff bundle of the winning design direction (F "Ateliê"): six `.dc.html` reference frames + `support.js` + token sheet. Snapshot — the living files are root `PRODUCT.md`, `DESIGN.md` and `packages/ui/tokens.css` |
| `plans/007-issue-14-plan-monorepo-foundation.md` | Implementation plan for #14 (monorepo foundation) |
| `plans/008-issue-41-plan-security-hardening.md` | Implementation plan for #41 (CI and security-header hardening) |
| `plans/009-issue-15-plan-anonymous-identity.md` | Implementation plan for #15 (anonymous identity: users/sessions schema, `POST /session`, web bootstrap) |
| `plans/010-issue-16-plan-binairo-engine.md` | Implementation plan for #16 (Binairo engine) |
| `plans/011-issue-22-plan-sudoku-engine.md` | Implementation plan for #22 (Sudoku engine) |
| `plans/012-issue-24-plan-nonogram-engine.md` | Implementation plan for #24 (Nonogram engine) |
| `plans/013-issue-26-plan-termo-engine.md` | Implementation plan for #26 (Termo engine and word-list harness) |
| `plans/014-issue-17-plan-publication-pipeline.md` | Implementation plan for #17 (publication pipeline: buffer, cron, published-predicate wall) |
| `plans/015-issue-39-plan-impeccable-ci.md` | Implementation plan for #39 (impeccable detect against Vercel preview deployments in CI) |
| `plans/016-issue-43-plan-dependabot.md` | Implementation plan for #43 (Dependabot for SHA-pinned actions) |
| `plans/017-issue-18-plan-play-the-daily-binairo.md` | Implementation plan for #18 (play the daily Binairo: play and conclusion screens, write-once completions, dormant hint grants, the apps/web db wall) |
| `plans/018-issue-23-plan-daily-sudoku-end-to-end.md` | Implementation plan for #23 (daily Sudoku end to end: the shared `apps/web/src/play/` layer, the Sudoku buffer and screen, device-local day state and conclusion chaining) |
| `handoffs/019-handoff-m2-nonogram-and-termo.md` | Session handoff for M2's remaining two games — #25 (daily Nonogram) and #27 (daily Termo): the shared play layer's real API and its honest limits, every extension point, and the decisions each game must make |
| `plans/020-issue-25-plan-daily-nonogram-end-to-end.md` | Implementation plan for #25 (daily Nonogram end to end: the Nonogram buffer and its contracts, the ruled variable-size board and its three-state brush, the picture reveal in the conclusion, and completion, hint, timer and offline sync at parity) |
| `handoffs/021-handoff-m2-termo-and-free-play.md` | Session handoff for M2's last two — #27 (daily Termo) and #28 (free play): what #25 changed in the shared play layer, where handoff 019 is now wrong, and Termo's two unresolved structural decisions (it cannot play offline, and ADR-0027's client-side hint argument does not transfer) |
| `plans/022-issue-27-plan-daily-termo-end-to-end.md` | Implementation plan for #27 (daily Termo end to end: the answer draw and its no-repeat rule, the stateless guess route and the offline degradation, the tile board and the pt-BR keyboard, the conclusion's fourth state, and the accent rule that closes #68) |
| `handoffs/023-issue-27-handoff-termo-mid-flight.md` | Session handoff taken mid-#27 with PR #77 open and steps 1–5 complete: what the ten commits landed, the three facts that cost that session real time (drizzle's insert column list, the `@miolos/core` ESM cycle vitest cannot see, zod's mutable tuple), the already-applied migration, and the known-deferred list step 6 must be shown |
| `handoffs/024-handoff-m2-free-play-the-last-ticket.md` | Session handoff after #27 merged and the daily Termo went live: what the review changed, the four seams #28 (free play) already has, the silent-green traps in `bundle-check` and `impeccable detect`, and the obligations #27 transferred rather than discharged |
| `plans/025-issue-28-plan-free-play-on-the-grid-games.md` | Implementation plan for #28 (free play on the grid games: the `/modo-livre` routes and level picker over the weekday ramps, browser-side generation with client-picked seeds, the free-play import wall and the evidence plan for the three negative acceptance criteria, and the route-scoped rework of the bundle markers) |
| `handoffs/026-handoff-m2-complete-the-frontier-opens.md` | Session handoff after #28 merged and free play went live, closing M2: the new machinery that must not break (route-scoped fail-closed bundle markers, the free-play wall's hoist-don't-copy rule, ephemeral-by-decision free play), the re-derived test-id frontier, and the open frontier with #58's pending decision flagged for whoever starts M3 |
| `plans/027-issue-19-plan-hoje-hub-streak-and-pwa-manifest.md` | Implementation plan for #19 (Hoje hub, streak, and PWA manifest: the `computeStreak` pure function and its alive-until-the-rollover anchor, the first authenticated read `GET /streak` and its client-fetched delivery to the hub stamp and the conclusion card, the free-play wall extension over the streak modules, and the manifest, icons and install metadata — with the day-truth payload deferral recorded as proposed ADR-0048) |
| `handoffs/028-handoff-19-merged-the-streak-is-live.md` | Session handoff after #19 merged and the streak, the first authenticated read and the PWA manifest went live: the authenticated-read conventions `GET /streak` sets, the reciprocal `Amended by:` rule and the other machinery #19 leaves armed, why M1 still has one ticket (#20, now unblocked), and #58's decided-but-unimplemented status the next streak-adjacent plan must respect |
| `plans/029-issue-20-plan-account-merge-recompute.md` | Implementation plan for #20 (account-merge recompute: the pure `mergeCompletions` union-earliest-dedupe in `packages/core` with its opaque ordering key, the idempotent no-transaction `mergeAccounts` operation on `@miolos/db/user` — sessions remapped never deleted, the loser emptied and retained as a tombstone, no migration — the deterministic winner rule, the lost-Termo collision carve-out, and the semantics recorded as proposed ADR-0049) |
| `handoffs/030-handoff-20-merged-the-account-merge-lands.md` | Session handoff after #20 merged: the two-seam merge machinery and its tripwires (the T-DB-S22 agreement bridge, the T-DB-S24 column-list tripwire, the strict-< tie rule), ADR-0049's amendment of ADR-0026, the #21 security preconditions recorded on the issue, the correction that M1 completes with #21 not #20, and the re-derived test-id frontier |
| `plans/031-issue-21-plan-email-attach-magic-link-merge-lgpd.md` | Implementation plan for #21 (email attach: the one request/confirm magic-link flow that serves attach, recovery, device move and merge-on-collision, the `attach_tokens` table and migration 0004 with the verified-email partial unique index, the winner-liveness guard discharging the merge-serialization precondition, timestamp-only consents kept per ADR-0022's conditional, the server-owned exactly-once prompt over a new `GET /attach/state`, Resend-via-fetch fail-closed transport, the `/privacidade` page with real self-service deletion, and the semantics recorded as proposed ADR-0050) |
| `handoffs/032-handoff-21-merged-m1-complete.md` | Session handoff after #21 merged, completing M1: the dormant-until-keyed attach stack and its security posture (cross-account session revocation closing the review-found takeover, the four recorded residuals), ADR-0050's flow-level amendment of ADR-0009, the preview-shares-prod-DB migration push-ordering rule, Fernando's activation checklist, and the re-derived test-id frontier |
| `plans/033-issue-29-plan-stats-calendar-dia-perfeito.md` | Implementation plan for #29 (stats, calendar, and Dia Perfeito: the pure derivations over one unfiltered reader — Dia Perfeito, the clamped account-birth calendar, the guess distribution with its unqualified fail row, the three-game times — the two closed contracts `GET /stats` and `GET /stats/calendar`, the `epochDay` hoist with its guarded inverse, the hub Termo tile's `em 4/6`, the conclusion stat block behind the recorded gate, the `/estatisticas` screen, and the semantics recorded as proposed ADR-0051) |
| `adr/0051-statistics-are-read-time-derivations-on-closed-contracts.md` | ADR-0051 (statistics are read-time derivations on closed contracts: no stored aggregate and no schema change, the account-birth calendar anchor with missed-by-absence and the clamped range edge plus its named #31 revisit, the two-endpoint split with the calendar payload's revisit trigger, three-game times with Termo's distribution, the `epochDay` hoist, and the three stat-row populations with their label obligation) |
| `handoffs/034-handoff-29-merged-m3-opens.md` | Session handoff after #29 merged, opening M3: the stats stack live in production (pure derivations on closed contracts, the won-only calendar clamp closing the review-found pre-birth-missed bug, the CI-caught mobile layout fix), the new stats read conventions and midnight-boundary rule, the `TURBO_CONCURRENCY=1` pre-commit landmine, the recorded residuals #30/#31/#37 inherit, and the re-derived test-id frontier |
| `plans/035-issue-30-plan-medals.md` | Implementation plan for #30 (medals: the 23 AI-curated definitions authored against `content/medals/README.md`'s written constraints with the ≥ 10-row rejected sample, the pure `earnedMedals` derivation over the unfiltered stats reader with the closed `MedalRule` union, the `medal_grants` table with its operator-ritual write path and the one-statement `least()` union-earliest-dedupe on merge, the shape-validated `GET /medals` id-set contract with the drop-unknown client rule, the nothing-at-zero row-list display on `/estatisticas`, the AC 3 tripwire triad, and the semantics recorded as proposed ADR-0052) |
| `adr/0052-medals-are-derived-facts-plus-curated-grants.md` | ADR-0052 (medals are derived facts plus curated grants: the two medal kinds and the curated-grant definition, recompute-never-store with the stored-rule-derived-id-ignored rule, the closed rule-input list, `medal_grants`' shape with the operator write path and the earliest-wins merge, the earned-id-set-only contract with shape validation and the `earnedDate` omission handed forward, the no-wallet tripwires with their named residual, ADR-0015 scaled to a hand-sized catalog, the ladder defence, the founder medal's #37 launch obligation, and the ×3 read-amplification restatement) |
| `handoffs/036-handoff-30-merged-medals-live.md` | Session handoff after #30 merged: 23 medals live on /estatisticas and GET /medals, the grants table applied pre-push, ADR-0052's amendment of ADR-0018, the VoiceOver and CLS-attribution review catches, the plan-register discipline rule, Fernando's four open flags, and the re-derived test-id frontier |
| `plans/037-issue-31-plan-archive.md` | Implementation plan for #31 (archive of past dailies, shipped as two pull requests: the write window's lower bound removed and replaced by a per-user-per-SP-day ceiling with the constant split into `ROLLOVER_SLACK_DAYS` and `isWritableDate`, then the `/arquivo` index, month, day and four literal play routes over three new wall readers and a DB-clock date classifier sharing one spelling of the publication conjuncts, `force-dynamic` for the kill switch with no index and no migration, the late result rendered in place with bounded local retention, pre-birth late completions off-calendar, the date-bearing CI URLs discovered from the app, and the semantics recorded as proposed ADR-0053) |
| `adr/0053-the-archive-is-a-public-past-only-read-and-a-late-write.md` | ADR-0053 (the archive is a public, past-only read and a late write: the four route families with today's date redirecting and the read-then-classify ordering, `force-dynamic` on the kill switch with `revalidatePath`-before-cache as the standing precondition, the data as its own floor, one spelling of the wall's conjuncts and of the SP clock with no index and a revalidate-first ordering rule, the write window's lower bound removed with what forgery actually buys accepted on ADR-0006's terms, the constant split one-owner-each, pre-birth late completions off-calendar, the unqualified fail row with its AC carve-out, no archive conclusion route, "you solved this" discharged at the write path, Termo included, gate URLs discovered not hardcoded, the chosen 50-late-writes-per-SP-day ceiling enforced inside the insert, which reverses ADR-0026's rejected rate limit for that branch, bounded play-record retention, and the two-PR deploy ordering as a rule — amending ADR-0008, 0014, 0024, 0026, 0028, 0038, 0039 and 0051) |
| `handoffs/038-handoff-31-merged-the-archive-is-live.md` | Session handoff after #31 merged as two sequenced pull requests and the archive went live: the public past-only read and the late write with its in-INSERT ceiling, the write window's split into `ROLLOVER_SLACK_DAYS` and `isWritableDate`/`isLateDate`, ADR-0053's ten reciprocal amendment lines across eight records, the four catches worth keeping as pattern (the probe that verified nothing, the check-then-act ceiling, the 429 that would have cost a streak day, and the two calendar bombs no commit could reach), the new landmine that a data- or copy-dependent gate goes red on the calendar, #96 filed and #34 unblocked, Fernando's eight flags, and the re-derived test-id frontier |
| `handoffs/039-handoff-post-31-the-gate-moved.md` | Delta session handoff over 038, covering the four PRs that landed after #31 without touching product code (#80, #98, #99, #100): CI made `pull_request`-only under a per-ref concurrency group so "CI green on main" is gone as evidence, the Vercel remote-cache token replaced by a branch-scoped `actions/cache` whose first run per PR is cold by design, the seven-day `minimumReleaseAge` and `trustPolicy: no-downgrade` that let `pnpm install` hard-fail on purpose with `minimumReleaseAgeStrict`'s inherited-vs-declared default as the trap, #31's eight flags recorded as confirmed in ADR-0053, the two Dependabot security settings enabled outside git, and what #34 inherits |
| `plans/040-issue-34-plan-sharing.md` | Implementation plan for #34 (sharing: the spoiler-free share text composed on the client from the local play record — coloured squares as content in a channel with no CSS, Termo's grid as the one game with a shape, and the answer, the streak, the medals and any claim about *when* the day was solved all excluded; one string through `navigator.share` with a clipboard fallback, falling through to the clipboard on any non-`AbortError` rejection; eight dated, `force-dynamic` Open Graph routes plus one committed static asset for the site card, over one card kind that reads the wall as an existence proof and draws no puzzle content — the daily four included, because the redirect-following scraper majority reads the daily route's head on the day people share; one wall read each, `getPublishedDaily` by date and `getTodayDaily` for today, so there is no midnight race; `openGraph` and nothing else on the daily routes, leaving their crawl-facing metadata byte-unchanged; committed static font instances at the app's own weight **and at the optical size the card is READ at rather than the size it is authored at**, because satori cannot read woff2, throws on a variable font, and synthesises no optical size; the standing OG lint wall written out so it cannot silently delete the db wall it overlaps; a database outage answering 500 while a bad row answers 404; and the semantics recorded as proposed ADR-0054, which grows **four** per-route enumerations — including the `revalidatePath` path list a future `killed_at` writer will be built from, and ADR-0039's fifth-game route recipe, which three consecutive audit passes cleared as untouched) |
| `adr/0054-the-share-is-plain-text-and-the-card-is-a-nameplate.md` | ADR-0054 (the share is plain text and the card is a nameplate: the composer inside the conclusion with no fifth prop, coloured squares as content in a channel with no CSS and no shape invented for the three games that have none, the exhaustive in/out list that keeps the answer, the streak, the medals, the hint count and any claim about *when* out of the share, one string through `navigator.share` falling through to the clipboard on every non-`AbortError` rejection, the per-day permalink as the share URL; eight dated OG routes plus a committed static site card, over one card kind — eight dated and `force-dynamic` because the redirect-following scraper majority reads the daily head — one wall read used as an existence proof with the card's date as its only field, a bad row 404ing while an outage still 500s, `openGraph` and nothing else on the daily routes so their crawl-facing metadata stays byte-unchanged, `OG_DEFAULTS` spread into every leaf because Next replaces rather than merges, three committed static font instances at the app's weight and at the optical size the card is READ at with the criterion that chose it, and the standing OG lint wall that repeats the db wall it overlaps — amending ADR-0028, ADR-0053, ADR-0039 and ADR-0043, with annotations on ADR-0045, ADR-0002 and ADR-0038) |
| `handoffs/041-handoff-34-merged-sharing-is-live.md` | Session handoff after #34 merged and sharing went live: the spoiler-free share text with Termo's coloured-square grid as the one game with a shape, eight dated `force-dynamic` Open Graph routes plus a committed static site card, and the metadata that lets a redirect-following scraper read a per-day image on the day people share. Carries what the eight steps cost — three plan-review rounds, six diff lenses, a two-pass fix and a verification round — and the four measured mechanics they turned up: a root-segment metadata module inflating every descendant's serverless trace, leaf `openGraph` replacing rather than merging, satori refusing px stops in a tiled gradient, and `css2` quantising `opsz` into buckets it serves silently. Plus the seven-times-repeated pattern of a record falsified by the work that cleared it, the two-way amendment check that finally caught it, and the root-`pnpm test` flake filed as #107 |
| `plans/042-issue-107-plan-test-timeouts.md` | Implementation plan for #107 (root `pnpm test` is a coin flip: explicit, arithmetic-documented timeouts on the three ESLint wall suites at file scope and on the two exposed Binairo properties per test, sized by anchoring on the highest **measured** figure — CI first, worst contended local second, never an isolated single-file run — times four and rounded up; the corpus sweep that gives the 40 %-of-applicable-budget trigger its warrant and names its three residuals; the concurrency cap deliberately split out after the plan's own arithmetic predicted a **+55 %** CI regression this pull request could not have measured honestly; and the semantics recorded as proposed ADR-0055) |
| `plans/043-issue-96-plan-archive-done-chip.md` | Implementation plan for #96 (the archive day page's per-game done chip: the repair of `useRecordSnapshot`'s single-slot cache into a bounded `(game, date)`-keyed `Map` — the mechanism the issue names, broken for the shape the issue asks for, and broken on the date axis too now that the date is a URL variable; the whole day card as the client boundary, because the anchor's `aria-label` replaces its content so the label and the chip must move together; an in-flow flex row beside the kicker rather than an absolute corner stamp, at a measured permanent cost of 8px per card on a dial that trades growth against the tape's clearance; `Feito` / `Jogado` — two verbs, because a lost Termo showing *Feito* is a false done — under the hub's on-time word for a late completion, with distinct done and played accessible names; no time, no result figure and no server read; and the semantics recorded as proposed ADR-0056) |
| `plans/044-issue-107-research-review-round-1-findings.md` | Round-1 review findings on plan 042 — a point-in-time record, kept as written including where the round was itself wrong |
| `plans/045-issue-107-research-review-round-2-findings.md` | Round-2 review findings on plan 042 — a point-in-time record, kept as written including where the round was itself wrong |
| `adr/0055-test-timeouts-are-sized-against-contention.md` | ADR-0055 (test timeouts are sized against contention: the 5000 ms default is not a budget, and the 40 %-of-applicable-budget trigger that replaces the ">10 s locally" one — swept on the `testTimeout` axis, and on the `hookTimeout` axis unswept as shipped but **narrowed at #114**, which instrumented the one hook population that mattered and left the rest unswept for the unchanged reason that vitest prints no hook durations; the anchor as the highest measured CI-or-contended-local figure with an isolated run never eligible and a run in which anything timed out never eligible either, one number per cost-driver population, ×4 retained for continuity with the shipped corpus rather than derived, and the sample-maximum residual written into the decision instead of around it; file-scoped budgets where a whole file shares one cost driver, safe because vitest gives every test file its own process; and a timeout as a ceiling rather than a target, with `budget / 2` — twice the sizing anchor, derived rather than picked — as a diagnose-and-record obligation, stated as a general rule and instantiated by the five drift triggers that actually move #107's numbers) |
| `plans/046-issue-96-research-review-round-1-findings.md` | Round-1 plan-review findings for #96 — five lenses, 5 × REJECT. A point-in-time record, never edited, not even to mark a finding fixed |
| `plans/047-issue-96-research-review-round-2-findings.md` | Round-2 plan-review findings for #96 — three lenses, 3 × REJECT. Same rule |
| `adr/0056-the-record-snapshot-cache-is-per-key-and-the-done-chip-wears-the-hub-word.md` | ADR-0056 (the record-snapshot cache is per key, and the archive's done chip wears the hub's on-time word: the single slot's cross-game AND cross-date loop, the count bound deleted after step 6 measured bound + 1 looping the same way, staleness swept off the poll as the only collector, and the two siblings deliberately not repaired; the `Conclusão tardia` / `Feito` collapse taken for cross-surface consistency and reversible in one line, with the two verbs not reversible and the stats-calendar counter-precedent engaged as record-versus-affordance; the done card's accessible name keeping a verb and the done and played names differing because the `aria-hidden` chip leaves the label as assistive tech's only channel; and every accent ring in a shared sheet enumerated by a scan rather than by a list of instances) |
| `handoffs/048-handoff-96-and-107-in-parallel.md` | Session handoff after #96 and #107 ran through the eight-step flow **simultaneously**, in two git worktrees, and both merged — with #107 deliberately left **open**, because its acceptance criterion is ten green runs on `main` and nine of ten reds were `@miolos/db`'s PGlite `beforeAll` on the `hookTimeout` axis ADR-0055 declared unswept (**narrowed at #114**, which instrumented that population and found the cause to be memory exhaustion under six-package fan-out rather than the hook), filed as #114. Carries what the parallel split did and did not buy (no shared file, one shared CPU, and a timing-sensitive ticket that cannot run beside anything), the four product questions answered in session, and the standing decision to merge on a green gate. Its durable finding is that a document's claims about itself rot faster than its claims about the code — nine more falsified records on top of handoff 041's seven, five of them rotted `file:line` citations on one PR, the last mis-adjudicated as correct after the sweep had surfaced it — and the three cheap mechanics that caught them: cite symbols rather than lines, sweep in both directions and never report a bare count, and verify recorded fixes against the tree rather than against the fix report. Plus the observation that every substantive defect this session was found by running something, while review-by-inspection caught records drift almost exclusively |
| `handoffs/050-handoff-114-merged-and-the-box-that-changed.md` | Session handoff after #114 merged as #117 plus the same-day correction #118, and #110 closed as delivered-by-#114 — with #114 and #107 both left **open** on one decision rather than on any work: the acceptance re-reading, since after the cap `pnpm test` no longer means what "default turbo concurrency" meant when either AC was written. Its durable finding is that **the reference box changed mid-ticket** (WSL2 raised 15 545 MB → 23 552 MB, from outside the ticket, in response to the very failures being diagnosed), which reversed three of the ticket's conclusions — uncapped is no longer red, `TC=4` is no longer faster than `TC=2`, and an eligible local hook anchor now exists where ADR-0057 decision 5 had argued at length that none did. The cap survives on a **weaker warrant stated as weaker**: it is retained for being the fastest and lightest setting, not because the machine exhausts memory, because 6 packages × `cpus-1` still puts 42 processes on 8 cores whatever the RAM. Carries the two config changes measured inert and reverted (`TURBO_CONCURRENCY=3` on `build`, whose graph has only 2 tasks, and on `typecheck`, whose 6 finish in 6 s), the first firing of a `fanout-cap` guard on a real change rather than its own fixture, the `--maxWorkers` knob measured as the best memory result and rejected because the root script is shared with CI, and an anchor arithmetic left **disagreeing with itself by one step on purpose** (CI 40 000 ms, local 45 000, shipped 30 000 retained on decision 4) |
| `plans/049-issue-114-plan-the-fan-out-cap-and-the-pglite-hook.md` | Implementation plan for #114 (the fan-out cap and the PGlite hook, absorbing #110): the diagnosis that the root suite's flake class is **memory exhaustion** rather than CPU oversubscription — six package suites at `cpus − 1` workers each demanding more RAM than the box has, and swap thrash turning a one-second WASM boot into a fifty-second one — with the boot's own phase split showing `initdb` as almost all of the cost and the migration replay as a rounding error; the concurrency sweep that picks **2** as simultaneously the greenest, the fastest and the lightest setting, shipped as an inline `TURBO_CONCURRENCY=2` on the root `test` script because that is the only form a caller's flag can override; CI deliberately held **uncapped and instrumented**, since the hosted runner's `maxWorkers` collapses to 1 and capping there would cost the gate step roughly half again; the timing instrument and the `turbo.json` env declaration without which turbo drops it silently; the hook budget kept at 30 000 ms with the arithmetic moved once to the cost driver and 26 sites pointing at it by symbol; the teardown registry proposed, measured and **dropped** — no `TypeError` ever fires, `isolate: true` bounds the leak to one file, and a killed hook's boot completes regardless, which no teardown can reach; **§2 as the tracked home for every figure** the ADR, the comment and the PR body state; and the semantics recorded as proposed ADR-0057 |
| `adr/0057-the-test-suites-memory-model-and-a-cap-that-binds-locally.md` | ADR-0057 (the test suite's memory model, and a cap that binds locally and not on CI: the failure as memory exhaustion with the model's shape recorded and its constant deliberately not, since a fleet average over six heterogeneous suites is not a per-worker bound; the cap at 2 as an inline env assignment rather than a flag or `turbo.json`'s global key, with the cores-to-RAM residual and the `poolOptions.maxForks` shape the next ticket takes stated inside the decision; CI kept as the one uncapped runner with the enforced `gate` context still at fan-out 10 and only the advisory pre-commit capped, plus the runner-upgrade tripwire; the boot instrument as what discharges ADR-0055 decision 1's unswept clause for this population and gives it a CI-first anchor; the budget kept at 30 000 ms with **the class avoided by the cap rather than closed** stated in those words, because a reader who takes "fixed" away will size the next budget wrong; and the abandoned-boot finding recorded with its own limits — a killed hook does not stop its boot, and that is explicitly not a cascade a teardown could fix — with the `isolate: true` dependency named beside it. Measurements deliberately absent, per ADR-0055's own rule; they live in plan 049 §2 and #114's PR body. Amends ADR-0055 across its decisions 1, 2 and 4 and its Consequences — enumerated there as a lettered mapping rather than as a count — and annotates ADR-0017 without amending it) |
| `plans/054-triage-the-open-tracker.md` | Triage of the twenty-five issues open on 2026-08-19, each read against the tree at `370f1e1` and sorted into the tiers [ADR-0058](./adr/0058-implementation-flows-are-tiered.md) defines: product 13, infra 9, records 3, and the finding that the tracker's composition was never the problem — the infra half kept spawning itself (#107 → #109 → #110 → #114 → #120 → #123 → #126) while nothing pulled from the product half. Carries the per-issue validity check against current code, which closed #76 (already fixed in `packages/core/src/testing.ts`) and #65 (a strict duplicate of #66), moved #58 and #59 to `ready-for-human`, and recorded that #111's body cited binairo's seed for Termo's and missed a third unsatisfiable assertion site; the case against building #78's citation checker, since the drift is four times worse than filed but the repo stopped writing the citation form the checker would police; and a recommended order of work that puts product first |
| `plans/051-issue-109-plan-the-three-timeout-residuals.md` | Implementation plan for #109 (the three timeout residuals ADR-0055 named and #107 filed without fixing: sudoku `P1 — determinism` **re-derived** 240 000 → 625 000 ms and sudoku `P3 — weekday ramp` **re-derived** 60 000 → 100 000 ms from their pooled CI maxima over seven genuine gate runs — on the warrant that neither number was ever derived by ADR-0055 decision 2 and decision 1's trigger fires on both (`P3` on two of seven samples including the maximum), so each is decision 2's first application with decision 4's diagnosis discharged first; the two siblings `P2` and full-week **retained** under decision 4 with `P2`'s one-step decision-2 disagreement recorded in the ADR-0057 decision 5 shape; `T-WEB-S43`'s clue rails closed **measured-and-fine with no timeout** — 944 ms CI maximum, 18.9 % of the default — with the pre-#120 local figures superseded rather than pooled, as a lettered decision-2 amendment, and the local axis re-taken under today's worker bound; the finding the issue did not have, that the sudoku figures **did** drift 4.4× on a byte-identical generator because on the 2-vCPU gate a CPU-bound test's wall time is set by the CPU share it gets beside whatever siblings execute concurrently, a share that saturates once the siblings outlast the file — `P1` 35 220 ms on #55's own gate at 39 test files, 155 025 ms two weeks later at 161, and 77 461 ms in between with more sibling files than a 124 686 ms run — so the successor is the gate's shape rather than a bigger multiplier, filed as a tracker issue and not left in a comment; the reading of which of ADR-0055's decision 2 and decision 4 governs a shipped ceiling, which ADR-0057 decision 5 asked the settling ticket to give, stated with decision 1's trigger as the discriminator and recorded as ADR-0055 annotation (p) with a reciprocal at ADR-0057; cost reduction assessed and rejected without touching any `numRuns`; the sudoku four adjudicated as four sites and not one population on stable ordering; the three merge surfaces with sibling #116 named with their resolution; and the records owed as a mapping — seven in-place ADR-0055 annotations (j)–(p) including the superseded "one green in sixteen" clause, the `vitest.shared.ts` cost-driver entry #120 owed, and the supersession rule for samples of a configuration nothing ships; the stale `T-WEB` frontier that #120's `T-WEB-S229` had outrun; and the file-header "90 s ceiling" the file measures at 137–250 s) |
| `plans/052-issue-116-plan-the-adr-status-lifecycle.md` | Implementation plan for #116 (the ADR status lifecycle: the set re-derived as **nine** rather than eight — ADR-0057 joins, as its own consequence (e) predicted — each flipped to `Accepted` with the merge date of the pull request that shipped it and that PR cited on the line as its provenance; the six lowercase `Status: accepted` + `Date:` headers normalized to the one bold shape so the acceptance grep sees all 57 files; the lifecycle rule for `docs/agents/domain.md` drafted in full — Proposed, Accepted (defined for ADRs with code and for decision-only ADRs, the shape of ADR-0001–0015, which are Accepted when the PR carrying them merges), Superseded whole-or-in-part reconciled with ADR-0001's existing header, Withdrawn, amendment as status-neutral, owner the agent driving the shipping PR with the flip written into that PR's diff at step 5, with the code, and the merge discharging it, checker the step-6 ADR reviewer and no mechanical checker with the reason and the reversal trigger written down; the two reciprocal `Supersedes in part` lines ADR-0003/0004 owed ADR-0001; the two-way sweep as a mapping, with ADR-0057 consequence (e) corrected by one tense parenthetical and every snapshot left as written) |
| `handoffs/053-handoff-109-and-116-the-residuals-and-the-records.md` | Session handoff after #116 and #109 both merged and closed on the same day — the ADR `Status:` field given a lifecycle, and the last three of #107's timeout residuals sized against measured contention. #116 flipped **nine** ADRs to `Accepted` rather than the issue's eight (ADR-0057 joined between the filing and the work, as its own consequence (e) predicted), normalized the six lowercase `Status: accepted` + `Date:` headers so the acceptance grep sees all 57 files, and wrote the rule — owner, trigger, four header forms, `shipped in #PR` provenance, amendment as status-neutral — into `docs/agents/domain.md`, which #109 then had to extend hours later, being the first ticket to amend an `Accepted` ADR at decision level with no ADR of its own. #109 re-derived sudoku `P1` 240 000 → 625 000 ms and `P3 — weekday ramp` 60 000 → 100 000 ms, retained `P2` and full-week under decision 4, and closed `T-WEB-S43` **measured-and-fine with no timeout at all**. Its durable findings are three rules and one habit: the **decision-2 / decision-4 partition** (ADR-0055 (p)), governing a budget being *set* versus every other shipped ceiling, whose load-bearing clause is that a decision-2-derived budget sits at 25 % of itself by construction, so decision 1's 40 % trigger firing on it is **expected** rather than a signal — the case the first draft left ungoverned; **annotation (o)**, that a sample taken under a worker configuration no shipped command reproduces is history and not an anchor — the single rule that turns `T-WEB-S43` from a 10 000 ms ceiling into none — with its two bounding clauses on what "shipped" means and that the configuration is the population's **own** package's; and the **gate-row regress**, hit live when the PR's own gate measured the rails at 990 ms above the 944 ms its comment called the maximum, where the fix was not to chase the figure but to write decision 2's own residual into the comment (the anchor is a *sample* maximum; an update is owed only when a sample crosses the trigger) — after which the next gate row came back at 666 ms and the clause held on its first exercise. Plus the habit that every sub-agent writes its report to disk before returning it, earned by two step-6 reviews that survived credit exhaustion beside an earlier batch of five that did not. Carries the reference box corrected to 16 GB (15 993 MB) against handoff 050's 23.5 GB, `packages/games` as the one workspace `vitest.shared.ts` does not bound, the worktree trap that fired on both merges, and the `CLAUDE.md` convention this session's own stale kickoff earned: the kickoff prompt is not snapshot content — it is regenerated with every addendum, lives after them, and opens with a preflight assertion |
| `adr/0058-implementation-flows-are-tiered.md` | ADR-0058 (implementation flows are tiered: the four tiers — Tier 0 just-do-it over a closed list of change kinds with no ticket, plan or reviewer, Tier 1 small fix with a reproduce-first bar and a single correctness lens, Tier 2 the eight steps unchanged, Tier 3 fog resolved into Tier 2 pieces — the routing rule that names the tier in the PR body before the work and allows escalation but never skipping, the artifacts-outweigh-diff test as the cheapest retrospective check, the no-observation-only-tickets rule with the napkin and ADR annotations as the replacement homes, handoffs at session or milestone boundaries rather than per ticket, records work as Tier 0/1 by default, and the mechanical gate stated as tier-invariant because it and escalation are the only two things bounding the honest cost — that a lighter tier can pass a change six reviewers would have caught. Ships with the `just-do-it` and `small-fix` skills under `.claude/skills/`, and without a ticket, under its own rules) |
| `plans/055-issue-126-plan-the-property-split.md` | Implementation plan for #126 (the ticket that ends the test-cost loop: the cost re-derived from four genuine gate runs as a per-file and per-test table, which puts **83–87 %** of `@miolos/games` in one file and **96 %** of that file in three properties — 300 sudoku generations on every pull request, markdown-only ones included, because the gate has no `paths` filter; the mechanism as an in-file environment variable, because ADR-0017 forbids a `vitest.config.ts` in that package and `T-WEB-S229` guards it, declared on `turbo.json`'s `test` task for `MIOLOS_TEST_DB_TIMING`'s own two reasons; the reduced sample sized at **25** by measurement rather than chosen — the reduced draw is a strict PREFIX of the full one, and full weekday coverage of the pinned draw first arrives at 21 runs, so a sample of 20 never draws Sunday at all; the split scoped to the measured cost centre and deliberately NOT extended to binairo, nonogram or termo; the nightly workflow with its `property-alert` issue and the `T-WEB-S231` scan without which deleting it would be silent; the release trigger recorded as ABSENT rather than invented, the repository having no tags and no releases; and the finding the issue did not have, that `@miolos/games` is **not** the gate's critical path — `@miolos/web` finishes 20–32 s later, so the `pnpm test` step is a makespan and the saving accrues to it only through released CPU share) |
| `adr/0059-the-property-proof-splits-from-the-per-pr-gate.md` | ADR-0059 (the property proof splits: ADR-0023's `numRuns >= 100` floor narrowed in **where** it binds and never in **whether** — the full proof moves to a nightly `properties.yml` under `MIOLOS_FULL_PROPERTIES=1` and the per-pull-request gate takes a 25-run sample of the same pinned draw, a strict prefix of it rather than a different sample; the mechanism forced in-file by ADR-0017 and kept out of `src/**` so the purity program never sees `process.env`, with `Math.min` so a property already at or below the sample can never be inflated and `=== "1"` so the flag fails closed to the cheap run; the sizing as three measured facts and not a speed target, with the weekday-coverage test as the tripwire that makes lowering it red; the split confined to a measured cost centre, so a new property joins it only with its own gate rows; the nightly's loud failure and the scan that keeps it from being deleted in silence; the pre-release trigger recorded as having no event to hang on; the five #109 timeouts left alone on purpose with the follow-up sweep's arithmetic written down instead; and the redundancy finding that the obvious one-character uniqueness break fails **no** test, because the grading ladder re-proves what the removal re-proof does — ADR-0023's construction layer working as claimed, and the reason the reduced sample loses nothing on that axis. Amends ADR-0023 at one clause and annotates ADR-0055 as (q) and (r), neither an amendment) |
| `plans/056-issue-83-plan-cross-device-day-state.md` | Implementation plan for #83 (cross-device day state, at step-4 revision: `GET /day` as a verbatim clone of the `GET /streak` authenticated-read template — no request parameters, the day being the DB clock's own São Paulo today, 401 `no-session` for a cookieless caller, `Cache-Control: no-store` on every branch, and the shipped route's two *deliberate absence* comments (no origin guard, no content-type check) carried across so a security reviewer reads a decision rather than an omission — carrying a new strict contract of three verbs per game and nothing else, no puzzle content, no duration, no guess count and **no `onTime` because nothing consumes it**, the tempting warrant *"on-time never rides a wire contract"* being false against `completionResponseSchema.onTime` and ADR-0051 decision 1's actual, narrower sentence; the merge discipline stated as an invariant rather than a heuristic — the server **makes a claim exactly when its status is not `pending`**, that `pending` being the absence of a completion row and therefore the absence of a claim and never a denial, so the server's claim wins wherever it makes one and the device's stands wherever it does not, nothing blended field by field, and the whole payload discarded unless its date equals the rendered day, which is the rollover mechanism and whose blast radius is larger than the `TermoDoneLink` gate it clones (a tile shape, not a caption); the exhaustive not-merged list (in-progress board state, the sync fields, durations, the streak, any day but today, the archive) and **three** consequences named up front — hydration can DEMOTE for the first time in the lost-Termo case *and the same one-line rule PROMOTES in the mirror*, where a Termo won elsewhere and lost here reads `Feito` one tap from this device's own loss screen; the server payload never written into a play record; and a cross-device done tile linking to a route that renders a fresh **playable** board in all four games, since the conclusion swap reads the local record — ADR-0053 decision 10 layer 3's *"honest gap"*, reached from the daily hub for the first time and pinned rather than assumed; `readDayState(date, server?)` keeping the shipped local projection as the offline fallback ADR-0031 decision 1 requires, with every failure path — env unset, 401, network, a deliberately silent parse failure matching both sibling clients — degrading to it and play never blocking on the fetch; one module-level `day-truth` store (named away from `serverDayState`, which already means the React SSR snapshot) whose hook takes **no argument**, because a `date` parameter would recreate ADR-0056's keyed-projection defect one level up, fetching on **listener count 0 → 1** and on visibility, focus and `online`, with cleanup dropping the listeners and retaining the payload, and NO interval — a decision with a named cost rather than a gate borrowed from ADR-0056, whose decision governs a `localStorage` poll and not a network one, the cost being that an already-open, already-focused tab never updates until it is refocused or navigated; no schema change, evidenced by the `completions_user_date_idx` comment that already names *"the day so far"* as one of the two reads it was built for, against a new narrow `listCompletionsForDay` reader rather than a whole-history one; `todayTermoGuesses` deliberately NOT carried, since a second producer of one value is worse than a second fetch and ADR-0051 decision 3's hub-endpoint trigger therefore stands, with the real arithmetic being two credentialed GETs on a normal hub view and three once Termo is done — a case this ticket makes more frequent — plus one on each conclusion surface; the nine source-prose sites this merge falsifies inventoried in the build sequence rather than left for step 6; and ADR-0060 landing **`Accepted`** in one PR for the merge discipline, amending ADR-0031 **decisions 1 and 2 and consequence (d)** — decision 2's never-overstate property surviving while its "absence renders pending" mechanism does not — while obeying ADR-0053 decision 10 and ADR-0056 decision 1 rather than amending either) |
| `plans/057-issue-35-plan-onboarding.md` | Implementation plan for #35 (M4 onboarding, **as revised at step 4** against a review that accepted it with three blocking findings): the "seen" fact as a nullable `users.onboarding_seen_at` rather than a table or a device store, because the acceptance's *per identity, surviving attach/merge* rules `localStorage` out and because the timestamp-with-NULL shape is already the repo's idiom for a once-per-account fact — merged by a new `mergeAccounts` statement 5d in statement 5b's `least()` idiom, guarded `is not null` + strict `<` so a re-run touches zero rows and `T-DB-S20`'s double-run snapshot stays green, with the loser's own value deliberately left on the tombstone, and carrying **both** twin columns because the review found the precedent this plan leaned on is itself unmerged — `attach_prompt_dismissed_at` has survived nothing since #21, filed as #134; the surface as **one in-flow paper card after the game cards, in the `HubAttach` slot**, which is the plan's own reversal and its most consequential change: the island materialises post-hydration after two sequential round trips, so above the grid its ~320 px would shift all four "Jogar hoje" links out from under a first-time player's finger on the one visit the ticket exists for — the acceptance criterion it most needs not to violate, and the reason `HubAttach` sits where it does and two shipped comments say *after the game cards* in those words; the mobile budget **measured in a browser rather than asserted** at **162.5 px of slack**, against the draft's "~250 px" and the review's hand-summed 188, with the card itself at 319.6 px and the shipped attach card at 484.8 px, so the honest conclusion is that no version of this surface fits above the fold and placement rather than height carries the decision; the overlay refused by four shipped comments and by the absence of any dialog, portal or focus-trap primitive in the repo, the route refused because a redirect is the literal blocking failure and deciding it server-side needs an identity `apps/web` structurally does not have; the whole nine-layer stack copied from the shipped attach-prompt precedent with **one departure that is the ticket's real finding** — on a genuinely first visit no `users` row exists until `POST /session` returns post-hydration, so a bare mount fetch races the mint into a 401 on the one visit the feature is for, and the hook awaits `ensureSession()` the way `play/sync.ts` and `termo/guess-client.ts` already do, with the four sibling hooks that share the race named rather than fixed; a `<section aria-labelledby>` with the invitation line as an `<h2>` instead of a second unnamed `complementary` landmark, which deletes the `aria` string outright, and a dismiss that moves focus to the first game card's link rather than dropping it to `<body>` and asserting that in the suite; the credentialed call measured anonymous on every `*.vercel.app` preview, so **Fernando cannot see this feature on a preview deploy** and that is the PR body's first line, with `pnpm dev` screenshots as the strongest evidence and the `file://` fixture — carrying `globals.css` and the real `@font-face` rules, and green only if `cream-palette` proves it loaded — as the second; the mobile gate replaced by a **direct measurement**, because `first-viewport-column-overflow` requires two side-by-side columns and is structurally incapable of firing on a stacked mobile hub whatever the card's height; forty-one words of pt-BR drafted in full against `CONTEXT.md`'s *sequência* and *virada*, four of them corrected once the review found the streak line false on the only visit the card ever renders on and the no-account line in collision with three shipped `sem cadastro` strings; one 200 ms settle whose reduce block stands down the transition and **keeps** the static rotation, because dropping the transform is a different design and not a calmer one; and the migration's Neon-before-first-push obligation as step 0, since a nullable column still changes the INSERT column list drizzle emits for `mintSession` and preview deploys share the production database |
| `adr/0060-the-day-payload-is-server-truth-and-the-device-may-only-add-to-it.md` | ADR-0060 (the day payload is server truth for today and the device may only add to it: `GET /day` with no parameters, anchored on the DB clock's São Paulo today, a verbatim clone of the `GET /streak` authenticated-read template down to its two *deliberate absence* comments, carrying a new strict contract of three verbs per game — `completed`, `played`, and `pending` as the absence of a completion row, the one verb CONTEXT.md has no entry for and which the contract adopts from the local projection rather than invents — and nothing else: no puzzle content, no day but today, no streak, no duration, no guess count and **no `onTime` because nothing consumes it**, the tempting warrant *"on-time never rides a wire contract"* recorded as FALSE against `completionResponseSchema.onTime`. The merge stated as an invariant rather than a heuristic: the server **makes a claim exactly when its status is not `pending`**, that `pending` being the absence of a row and therefore the absence of a claim and never a denial, so `merged[game] = server[game] === "pending" ? local[game] : server[game]` with the whole payload discarded unless its date equals the rendered day — which is also the rollover mechanism, and whose blast radius is a tile SHAPE where the `TermoDoneLink` gate it clones discards a caption. Not "the stronger verb wins": the exhaustive not-merged list, the server payload never written into a play record, one shared module-level store fetching on **listener count 0 → 1** and on visibility/focus/`online` with cleanup retaining the payload and — since #143 amended decision 5's original NO-interval clause (annotation (a); its named cost, an already-open, already-focused tab never updating until it is touched, was the revisit trigger and it fired when Fernando asked) — a 60 s poll that runs only while a listener is subscribed and the tab is visible, every tick through the same in-flight guard; still not a gate borrowed from ADR-0056, which governs a `localStorage` poll and is obeyed rather than amended, as is ADR-0053 decision 10. Every failure degrades to the local reader and play never blocks on the fetch; hydration may DEMOTE in exactly one case, a Termo won here and lost elsewhere, with the same one-line rule PROMOTING in the mirror where `Feito` sits one tap from this device's own loss screen; and a cross-device done tile keeps its `href` onto a route that renders a fresh **playable** board — ADR-0053 decision 10 layer 3's *"honest gap"*, reached from the daily hub for the first time. Amends ADR-0031 **decisions 1 and 2 and consequence (d)**, with decision 5 discharged: decision 2's never-overstate property survives while its "absence renders pending" mechanism does not. **Amended at #141**, annotations (b)–(e): a completed grid game's claim now carries the stored `elapsedMs` — a consumer exists, the hub tile — while Termo still publishes no duration anywhere and the guess count still rides `/stats` alone) |
| `handoffs/058-handoff-the-loop-and-the-tiers.md` | Session handoff after the night ordinary ticket work turned into a course correction: of the eleven pull requests merged 16–19 August exactly **one** (#113) contained product code, and five test-infrastructure tickets had been spawning each other — #107 → #110 → #114 → #120 → #109 → #123 — each moving a wall (timeout values, turbo's fan-out cap, vitest's worker bound, timeout values again) and none touching the cost, which was `sudoku/generate.test.ts` at 233 s of a ~4 min gate because `P1` generates 200 sudokus and the gate has no `paths` filter, so a markdown-only PR paid for 300 generations. Ten PRs merged (#127–#133, #135–#137), nine issues closed (#123 as a filing error named, #76 premise-false, #65 duplicate, #111, #63, #67, #103, #126, #83), two opened (#126, shipped and closed the same session; **#134**, `attach_prompt_dismissed_at` not merged on account merge, a real defect in shipped code), 25 open issues down to 18. The tiers land as ADR-0058 — four rows, the routing table, the tier named in the PR body as a checkable claim, *if a ticket's process artifacts outweigh its code diff it was the wrong tier*, no observation-only tickets, handoffs at session close, escalation allowed and skipping never, records work Tier 0/1 by default — with two new repo-versioned skills (`just-do-it`, `small-fix`) and **the gate binding identically at every tier**, which is the whole reason the light tiers are safe; and the honest note that this session's own #35 plan reached 1 796 lines for one dismissible card and fails rule 2, with the weight accumulated in the review round rather than the first draft. The property split (ADR-0059) measured: file 233 → 103 s (−56 %), `@miolos/games` 274 → 157 s (−43 %), step 5m06 → 4m44 — with the caveat the ticket itself insisted on, that `@miolos/games` is **not** the critical path (`@miolos/web` finishes 122 s later), so the saving reaches the step only as released CPU share. Five durable findings: **(1)** a timeout figure means nothing without naming its axis, now demonstrated rather than inferred — the nightly runs the *same* 100-run floor in 30.8 s that the shared gate ran in 105–155 s, because it runs `packages/games` alone, so the full proof is now cheaper than the reduced sample was; **(2)** the test-id frontier's structural limit — re-deriving prevents a *stale* allocation and **cannot** prevent a concurrent one, four tickets colliding on one `T-WEB` row in one night each having re-derived correctly and #83's range renumbering three times, mitigated only by reserving the id on `main` before step 5; **(3)** a conflicting PR produces **no** `pull_request` gate run at all and CI silence reads exactly like a queue delay (70 minutes queued on `mergeable=UNKNOWN`), so diagnose with `gh pr view --json mergeable,mergeStateStatus` before re-pushing; **(4)** sub-agent reports are written to disk before being returned — two reviews survived credit exhaustion because of it, an earlier batch of five lost work to a process restart because they had only composed final messages; **(5)** the box lock serialising suite runs across worktrees, a capped suite being ~6.3 GB on a 16 GB box against the uncapped path's 11.6 GB, with the admission that the test-infrastructure investigation itself was the most memory-intensive thing being done. What shipped as product: #63 (the clock stops shifting — Fraunces answers no OpenType tag at all, two changed lines, and the sweep found the stats readouts already correct), #67 (hint tab order — the issue's "one CSS edit" framing wrong because **no CSS property reorders sequential focus**, so the node moved and zero CSS changed, across six screens rather than three), #103 (archive share — the late-share decision being that it says exactly what an on-time share says, byte for byte), #83 (cross-device day state: `GET /day`, the merge invariant, ADR-0060). Landmines carried: the four #83 decisions shipped for veto in PR #135, #83's own residuals (an already-open focused tab never self-updates, a cross-device done tile links to a fresh playable board, the Termo demotion and its mirror promotion), and the sudoku budgets left un-re-derived until three genuine post-#126 gate rows exist to pool |
| `adr/0061-onboarding-seen-is-a-users-timestamp-merged-earliest-wins.md` | ADR-0061 ("seen the introduction" is a `users` timestamp, merged earliest-wins, never device storage: the first-visit onboarding's once-per-account fact as a nullable `users.onboarding_seen_at` — the shipped timestamp-with-NULL idiom, chosen over a table (union + tombstone delete + a join for one bit) and over any device store, which survives neither cleared site data nor attach/merge, the two things #35's acceptance requires; merged by `mergeAccounts` statement 5d in statement 5b's `least()` idiom under an `is not null` + strict `<` guard OR-ed across two columns, so either side suffices, both-set keeps the earlier, a re-run matches zero rows, and the same statement carries `attach_prompt_dismissed_at` — closing #134, the shipped precedent's own unmerged gap; the first `users`-COLUMN merge duty and the first winner-side `updated_at` write, annotated on ADR-0049 decision 6 rather than amending it; the fact server-owned end to end through one derived boolean (`show`) and one empty-bodied idempotent POST, the timestamp never crossing the wire per ADR-0048 decision 3, with the honest failure direction stated — an unreachable server never nags) |
| `adr/0062-termo-guess-dictionary-gaps-close-with-curated-additions.md` | ADR-0062 (Termo guess-dictionary gaps close with curated additions, not a hunspell expansion: #140's rejected *áudio* was a membership gap in the IME-USP lexicon — `validation.txt`'s sole source, explicitly "possibly incomplete" — and a corpus sweep showed a class, not a word (*podre*, *letal*, *doces*, *tchau*, *dócil*, *golfe*, *suíte*…); closed by `content/termo/additions.txt`, one canonical per line, sorted by (normalized form, canonical), merged by `pipeline.py` under the same normalization and blocklist as lexicon entries with a loud failure on any line the base lexicon already carries; the admission criterion recorded so the next pass can repeat it — corpus-attested at aggregated frequency ≥ 200, missing, and a real, current, inoffensive pt-BR word, loanwords only when Brazilian dictionaries register them; 98 words passed, 5 310 → 5 408; the hunspell/unmunch expansion rejected for now on offline-verifiability, not principle, with the stale-line assertion already handling the overlap the day it lands; guess dictionary only — `answers.csv` untouched under ADR-0015) |
| `plans/060-issue-142-plan-cross-device-completed-view.md` | Implementation plan for #142 (a cross-device Feito opens a completed view, not a fresh playable board — **as revised at step 4**: the scope decided from server-side evidence as the evidence-bounded middle, full where the write-once completion row can back it and honest absence where it cannot — grid games get the real stamp with the server-held time and hint count, stats, streak, day card and next-pending CTA; a completed Termo gets the win stamp with `em X/6` from ONE `GET /stats` plus the guess distribution, never the unstored guess grid or the day's word; a played Termo gets the loss shape with nothing fabricated — carried by one wire field (optional `hintsUsed` on the per-game claim, the #141 template exactly, `.max(1)` mirroring the write contracts, Termo-suppressed at the producer, no migration), the `useServerDayClaim` seam keeping ADR-0060 consequence (d)'s single-importer rule true, `RemoteConclusionView` co-located in `conclusion-view.tsx` because a two-file split would import in both directions, the hook hoisted to the top of each screen root by the rules of hooks with only the branch after `isClosedAndFrozen` so a local closed record always outranks the claim, the per-line skew stamp that renders the time it has and omits the hints line rather than fabricating a `sem dicas`, the claim replacing `syncOutcome === "recorded"` as the stats/streak gate, the mid-play swap with its named cost, no replay and nothing written into local play records, `T-WEB-S245`'s playable-board arm deleted with `T-WEB-S273` as successor, and ADR-0065 amending ADR-0060 decision 8's playable-board sentence while its href half stands) |
| `adr/0065-a-cross-device-done-day-opens-a-completed-view.md` | ADR-0065 (a cross-device done day opens a completed view, not a playable board: the evidence-bounded scope table with its server-storage warrant — the completions row holds outcome, time, hint count and Termo's guess count, and nothing else, so full Termo (guess grid + answer word) is named as a different ticket rather than smuggled in; one wire field, optional `hintsUsed` on the per-game claim under the #141 template with the write-contract cap mirrored and the Termo suppression kept; the seat as a sibling branch in the four screen roots plus the `/​<jogo>/concluido` empty branch, the local closed record winning first; the per-line stamp rule — a deploy-skew claim renders its time and omits the hints line, never a fabricated value; stats and streak gated on the claim itself, strictly stronger evidence than `recorded`, with one lifted `GET /stats` per remote view; the mid-play rule with its named cost, no replay and no local-record write, the #145 push opt-in card's exclusion stated for its reviewers, and the credentialed-GET consequence in ADR-0060 consequence (a)'s sizing language — the four play routes go from zero `/day` calls to one plus the 60 s visible-tab poll, and a mounted remote view adds one `/streak` and one `/stats`. Amends ADR-0060 decision 8's playable-board sentence (annotation (g); the href half stands, which is the one-line `Amended by` warrant) and decision 2 (annotation (f), `hintsUsed`); ADR-0053 decision 10 is obeyed, not amended — the archive keeps its honest gap) |
| `plans/061-issue-145-plan-web-push-opt-in.md` | Implementation plan for #145 (Web Push opt-in; a step-2 snapshot, per its own header, that already carries the step-4 revisions. Drafted as plan 058 — a number `handoffs/058-…` already held — and renamed to the next free number at step 7; the ADR it drafts as "ADR-0062" shipped as ADR-0064, #140 having taken 0062 in #152. Ships: migration 0007 — `push_subscriptions` with the endpoint PK and created_at-as-consent, plus `users.push_prompt_dismissed_at` — applied to Neon before the branch's first push; the caching-free service worker with exactly two handlers as the ADR-0004 discharge, its pt-BR fallbacks recorded as the one i18n exception; `GET /notifications/state`, `POST /notifications/dismiss` and `POST`+`DELETE /push/subscriptions` behind the fail-closed `isPushConfigured` triple; `pushOptInStreakThreshold` (default 3) in remote config; merge statements 1b and 5d's third column with the falsified-record sweep; the conclusion-surface pre-prompt card behind the triple feature detect with register → `serviceWorker.ready` → subscribe inside the gesture, decline and denial stamping permanently and transient failures stamping nothing; `web-push` deliberately NOT installed in this slice; and the semantics recorded as the #32 shape's ADR) |
| `handoffs/062-handoff-the-answer-batch.md` | Session handoff after the pending-on-Fernando ledger shipped (PR #157) and Fernando's answer batch was executed: VAPID live, `CRON_SECRET` set, #32 answered 1a/2a, #64/#104/#158 decided and ready-for-agent, #74's recommendation standing, and #59 authorized but waiting on a Fernando-present session |
| `handoffs/063-handoff-the-three-activations.md` | Session handoff for 2026-08-21, the day three deployed-but-dark production surfaces were proven working and `docs/pending-fernando.md` NOW went **empty** for the first time: `CRON_SECRET` rotated (it is a Vercel **Sensitive** variable, so its two holders can never be copied into one another) and the streak-notify tick green on its first-ever `success`; `POSTHOG_KEY` set and one clean `puzzle_started` captured with person-profiles off, GeoIP off and no client SDK; and Resend/email-attach activated end to end — first magic link (inbox, not spam), first live `mergeAccounts` (older holder wins, loser tombstoned, other sessions killed) and first account deletion (row gone, cascade clean). Its through-line is the `turbo-ignore` trap that hit all three: a Vercel env var reaches the running function only on a deploy with a real diff under `apps/api`, and `vercel env ls` cannot tell you whether that happened. Also records the scratch cleanup that rescued plans 066/067 and destroyed `gate-lock.sh`, now rebuilt into `scripts/` |
| `adr/0064-streak-at-risk-is-a-derived-decision.md` | ADR-0064 (streak-at-risk is a derived decision: the habitual window as a read-time SP median, never stored; per-endpoint subscriptions with created_at-as-consent and the merge remap, NOT revoked on merge; the one-lifecycle prompt stamp merged earliest-wins as statement 5d's third column; the fail-closed VAPID-triple dormancy predicate with the public key riding the state response; the caching-free service worker as the ADR-0004 discharge with register → ready → subscribe inside the gesture; the dispatcher as the product's only notification code path with the claim-first `notification_sends` ledger and its priced residual; the hourly GitHub Actions tick over Vercel Hobby cron; `web-push` as the one composition-crypto exception, taken in apps/api only with its first caller (#146); and endpoint pruning on 404/410 — amending ADR-0061's column enumeration) |
| `plans/059-issue-58-plan-late-sync-seen-days.md` | Implementation plan for #58 (the late-sync seen-days rule, **as revised at step 4**; the plan executes Fernando's 2026-08-02 decision verbatim rather than reopening it: a server-owned `user_seen_days` row per (user, SP day) written by the session service's three authentication hooks, a completion for `D` syncing after the rollover credited to `D` iff that row exists and `D` is exactly one day back — `onTimeAtWrite` in packages/core, the 1-day window as its own constant with one owner; on-time decided ONCE at write and STORED on the completion row because ADR-0009's derivability constraint forces storage the moment a second table enters the definition, every reader flipped to the stored column and `onTimeSql()` deleted; migrations 0008 (pre-push: table + column with a temporary default + one-directional promote-only backfill) and 0009 (post-deploy: the same sweep + DROP DEFAULT) with the visible window stated honestly; the credited write exempt from the late-write ceiling; the multi-past-date guard as a 422 widening tripwire, dead code by design under window = 1; the bounded-retention delete on the existing publish cron; the merge union with the accepted downgrade corner pinned; no client change, verified rather than assumed) |
| `adr/0066-a-late-sync-is-credited-from-a-server-seen-day.md` | ADR-0066 (a late sync is credited from a server-seen day; on-time is stored at write: the seen table, its three session-hook write points and its cron retention; the pure write-time rule with the 1-day credit window; the invariant sentence — the table is consulted exactly once per completion, at write time, so a streak stays derivable from completion rows alone and ADR-0009 is untouched; the credited write's ceiling exemption with the 429-loses-the-day reasoning; the guard's per-writing-day reading of "one sync" and its fold-into-the-insert revisit trigger; storage-as-authority migrations with the honest visible window; the merge union and the falsified never-downgrade theorem kept earliest-wins. Amends ADR-0026 — decision 2 and the stored-boolean Rejected entry reversed, decision 7 resolved — ADR-0008's derivability sentence and on-time verb, and ADR-0009 additively) |
| `plans/063-issue-146-plan-streak-at-risk-dispatcher.md` | Implementation plan for #146 (the streak-at-risk dispatcher, **as revised at step 4**: migration 0010 — `notification_sends`, the claim-first ledger with the composite (user, SP day, channel) PK, applied to Neon before the branch's first push; the candidate reader as one SQL statement over the DB clock's one-snapshot `{today, hour}` — `percentile_disc(0.5)` over per-counted-day earliest SP hours, floored deliberately (half-up fires the 23:40-habit nudge at the 00:xx tick, ~24 h early), a prefilter that never re-defines the streak; `POST /cron/notify` behind the extracted shared `isAuthorized`, 503-before-any-DB-read dormancy, 200-with-failures; the hourly zero-marketplace-actions GitHub Actions tick; `web-push` sends with TTL 3600 carrying `computeStreak`'s number in pt-BR copy; 404/410 pruning; the scoped-cap 10-endpoint write-side subscription ceiling with its 429; `mergeAccounts` statements 5e/5f; and ADR-0068 recording the six operating decisions. Q1 = 1a and Q2 = 2a, Fernando 2026-08-20 on #32) |
| `adr/0067-termo-accent-deepens-to-carry-a-light-label.md` | ADR-0067 (the Termo accent deepens to carry a light label, #161: `--accent-termo` `#C08A1E` → `#8D6212` — same ~40° hue, luminance moved inside the family band — and `accentVars("termo")` flips `var(--ink)` → `var(--paper-desk)` at a measured **4.8433:1**, so all four hub cards carry the family's deep-fill/light-label treatment; supersedes ADR-0041 decision 6 through the new-ADR instrument that decision itself demanded, dissolves consequence (c)'s Termo-only `var(--ink)` fallback, opens no accent-as-text site (the board's ink glyphs stay as a kept judgement), moves every token mirror in the same diff (OG literals under T-WEB-S200's leading-seven tie-back, the icon SVG + re-rendered PNGs, the impeccable waiver reason) while snapshots keep the old hex, and adds the T-WEB-S299–S301 token-resolving contrast pins so a future nudge reds arithmetic rather than trusting a table) |
| `plans/065-issue-163-plan-archive-calendar.md` | Implementation plan for #163 (the archive month view as a real clickable calendar, **as revised at step 4**: both archive surfaces replace their day-row lists with one shared server-rendered zero-client-JS month grid — the month page's rows become the grid, the index shows the newest archived month's grid over a widened 124-row bounded read with the "Por mês" chips kept as the impeccable workflow's URL-discovery source, now test-pinned; ≥44px square `<Link prefetch={false}>` tiles for published past days named "15 de agosto de 2026 — sábado", aria-hidden inert numerals for everything else with no clock read anywhere; the Sunday-first pt-BR weekday header as two `messages` 7-tuples, no Intl weekday formatter; the month geometry extracted once to `src/calendar/month-grid.ts` with the stats API unchanged and its tests unmodified; `DayRows`/`recentDayGroups` deleted; the seasonal impeccable worst case — 6-week 2026-08, fevereiro heading — unit-pinned before the preview scan; no ADR, per D6's sentence-level ADR-0053 audit) |
| `adr/0068-the-dispatchers-operating-decisions.md` | ADR-0068 (the dispatcher's operating decisions: the per-user subscription ceiling of 10, write-side, folded into the subscribe upsert's own INSERT with the `exists` disjunct scoped to the caller's (endpoint, user_id) — at the cap a new endpoint and a cross-user repoint both 429, with the READ COMMITTED overshoot and the at-cap-repoint refusal stated as the honest residuals; the `notification_sends` merge duty as union-then-empty (statements 5e/5f, ADR-0049 decision 6's extension point, presence-is-presence on a PK collision); the one-snapshot tick instant `readTickInstant` — SP day and hour in one statement, parameters all the way down, the testability seam that keeps "DB clock only" literally true; POST + 503-when-unconfigured + 200-with-failures status semantics with the per-line log as the observability channel; serial sends with TTL 3600, the ~150-candidates measured revisit trigger, and the crash-window, unclaimed-tail and skipped-tick residuals priced; `pushsubscriptionchange` deferred to #36; and the candidate SQL's counted-day prefilter recorded as a deliberate duplication with `computeStreak` the only streak authority, including the narrowing of ADR-0064 decision 1's sample to won∧on-time rows) |
| `plans/064-issue-33-plan-posthog-telemetry.md` | Implementation plan for #33 (PostHog telemetry, **as revised at step 4**: no client SDK at all — a hand-rolled server-side capture in apps/api posting the five events to the US ingestion endpoint; four events at their server truth seams (`puzzle_completed` and a derived-on-return, at-most-once-per-break `streak_broken` in POST /completions with the max-counted-date guard that kills the newest-first-flush double-fire; `notification_opt_in` on a genuine first insert, `upsertSubscription` answering `{stored, inserted}`; `login_linked` on attach confirm keyed by the resolver's `winnerId`), and `puzzle_started` — the one event with no server fact — over a Zod-parsed first-party POST /telemetry relay with server-derived `archive`; free play silenced structurally AND by a named ESLint-wall extension; `distinct_id` = the existing server `userId` with no new identity surface and no privacy-copy change; the `POSTHOG_KEY` production move shipping as a wizard-ready pending-Fernando NOW item in the same PR; ADR-0069 recording the posture and its residuals) |
| `adr/0069-telemetry-is-five-server-anchored-events-over-a-hand-rolled-capture.md` | ADR-0069 (telemetry is five server-anchored events over a hand-rolled capture: no SDK and no dependency, so the five-event ceiling, no-autocapture and the published no-replay promise hold by construction rather than by an eleven-flag kill config; four events fire inside their authoritative routes and only `puzzle_started` rides the closed single-literal `POST /telemetry` relay, every capture post-response inside `after()` and unable to delay or fail a route; `streak_broken` derived on return under four conditions with its claim bounded to "at most once per break, exactly once in the sequential single-client case" and three residuals priced — retro-close, two-device race, return-time timestamps; archive plays distinguished by the stored verdict (`on_time`, server-derived `archive`) while free play fires nothing, wall-enforced by name; `distinct_id` = the server `userId` with no device id, no new cookie and no copy change, and PostHog-as-third-party-processor named as the residual #37's LGPD review starts from; `notification_opt_in` on `inserted` and not `stored`, by a pre-read fallback after `(xmax = 0)` failed to type through the `Db` union; the unthrottled relay's quota exposure and the 204-drop-on-no-session accepted and named; a missing `POSTHOG_KEY` a valid but LOUD state; and error tracking explicitly left to #37 — with the archive module-graph wall recorded as the reason `usePlayLifecycle` takes the day claim as an input rather than reading it) |
| `plans/066-issue-64-plan-nonogram-motif-name.md` | Implementation plan for #64 (name the revealed Nonogram picture on the conclusion, **as revised at step 4 after a REJECT verdict**: the name delivered as an optional field on the `/day` per-game claim — the ADR-0065 decision 2 `hintsUsed` template verbatim — so it is post-completion by construction, survives reload, reaches a second device through the seam every conclusion already reads, and needs no new endpoint; the completions response and a new post-completion route both rejected with reasons that still hold in code; ships ADR-0070 assigned up front rather than "next free at merge", and amends ADR-0033 in the same diff. **Preserved 2026-08-21 unedited from a session scratch directory that was never committed** — a point-in-time snapshot rebased against `main` `600d640`. #64 is still open and `ready-for-agent`, with Tier 2 steps 1-3 already done here) |
| `adr/0070-the-daily-nonogram-conclusion-names-its-motif.md` | ADR-0070 (the daily Nonogram conclusion names its motif, over the wire — superseding ADR-0033 decision 1's name clause for `reveal.name` only, on the exact path ADR-0033 decision 5 prescribed: an optional `motifName` on the user's own completed `/day` claim, post-completion by construction because a claim is a projection of that user's own completion rows; the field spelled `motifName` so ADR-0033 decision 4's rename route is taken and `FORBIDDEN_DAILY_KEYS` keeps passing on merit; the publication rule enforced at both ends — a parse failure on any non-`completed` claim, and a producer that scopes it to nonogram; the wall read `getPublishedNonogramMotifName` on the publishing entry only, never throwing and normalising a blank stored name to `undefined` because `nonogramRevealSchema` has no `.min(1)` and one such row would 500 the whole day payload; the composed description kept as the honest degraded label offline, pre-sync and on a killed row; the archive, free play and the cross-device remote view recorded as non-goals with the remote view's name joining ADR-0065's honest-absence table; a game-blind recorded-sync refresh nudge fired from the conclusion and never from `sync.ts`, which is in the archive's module graph; and — against the instruction that authorised spending it — **the bundle tripwire KEPT**, its warrant rewritten, because the grep scans `.next/static/chunks` only and an API response is never a chunk) |
| `plans/067-issue-32-shape-streak-at-risk-notifications.md` | Tier 3 shape for #32 (streak-at-risk notifications; the read-only fog-clearing pass of 2026-08-19 that produced #145 and #146, both since shipped). **Preserved 2026-08-21 unedited from a session scratch directory that was never committed.** Kept because #32 remains open: the **email-hedge slice** it describes is the residual work, and the Resend transport that slice waited on went live 2026-08-21 |
| `plans/068-issue-104-plan-archive-og-cards.md` | Implementation plan for #104 (OG cards for the archive index, month and day pages, **as revised at step 4 after three step-3 REJECTs and corrected at step 5 from the measured spike**: the route shape decided against the serverless trace rather than against the convention — a committed `app/arquivo/opengraph-image.png` for the dateless index, and the two dated cards moved out of their segments to `/cartao/<YYYY-MM-DD>` and `/cartao/mes/<YYYY-MM>` with an explicit `openGraph.images`, which keeps all three archive shells at a measured 2.7 MB unchanged to the file where a segment module would have put ~23 MB back on each; the day card as a dated nameplate that names no game, on the ragged floor, the rule that a shared accent may never colour a word, and `archiveCard`'s two-string signature as the structural guarantee rather than `limit: 1`; existence as the DAY through one bounded call to an existing reader with no new reader, no clock and no `packages` diff; no `try`/`catch` in either handler, argued rather than omitted; the measured one-date residual on `/arquivo/<hoje>` accepted with its three fixes named and refused; and the semantics recorded as proposed ADR-0071, which amends three records) |
| `adr/0071-the-archive-cards-are-nameplates-and-two-leave-the-file-convention.md` | ADR-0071 (the archive cards are nameplates, and two of them leave the file convention: one third builder in `src/og/card.tsx` rather than a new system; the index card a committed static asset because it reads nothing, doubling as the degraded-path card a malformed `[data]`/`[mes]` segment now inherits, and the two dated cards `force-dynamic` handlers at their own pt-BR root URLs — ADR-0054 decision 9's deferred non-segment relief taken for these three routes only, measured at 2.7 MB unchanged against ~23 MB per re-inflated page, with the eight game cards' residual left #37's; `cartao` as a chosen slug that obeys ADR-0013 rather than excusing itself, and the nested `/arquivo/cartao/**` family refused on the forward argument that only a root family can later host the daily four; each card's existence proof its page's through one bounded `listArchivedDays` call, Zod before any read, a bare 404 with the same `cache-control` as the 200, and no catch because a narrowed one would be unreachable; the day card naming no game, with `archiveCard`'s signature as the guarantee and `limit: 1` explicitly not it; and the measured residual on `/arquivo/<hoje>` — the 307 carries a 12.5 KB `<head>` — accepted as a one-date, self-healing, generic-card-to-no-card degradation. Amends ADR-0054, ADR-0053 and ADR-0028) |

## Language

English for filenames, new documents, code and commits. pt-BR for user-facing product content, and for the two founding documents above, whose **bodies** stay as written — corrections are appended as an amendment table, never edited inline.
