# Docs

Project documentation. Two kinds of file live here, and they follow different rules.

## Living documents (no number)

Read them for the current state of the project. They are edited in place.

| Path | What it is |
|---|---|
| `agents/issue-tracker.md` | Where issues live and how agents operate on them |
| `agents/triage-labels.md` | The five canonical triage roles → this repo's label strings |
| `agents/domain.md` | How agents must consume `CONTEXT.md` and ADRs |
| `agents/test-ids.md` | The `T-<AREA>-[S]<n>[<letter>]` convention, the per-area frontier and the burned slots |
| `adr/` | Architecture Decision Records (`0001-…`, own 4-digit sequence) |

`CONTEXT.md` (the domain glossary) lives at the repo root, not here — see `agents/domain.md`.

## Sequenced artifacts (numbered)

Handoffs, briefs, plans and specs are **snapshots of a point in time**, not living specs. Their bodies are never rewritten to stay current: a newer document supersedes an older one, and where a decision changes part of one, an **amendment table is prepended** pointing at the ADR that supersedes it. Never edit the body to match a later decision. The number tells you the order they were created and used.

```
NNN[-issue-<n>]-<type>-<slug>.md
```

- **`NNN`** — a single sequence shared across every subdirectory, so creation order stays readable no matter where a file sits. Next free number wins.
- **`issue-<n>`** — optional. Present only when the document belongs to exactly one GitHub issue. Omitted for documents written before any issue exists, and for documents that span a whole milestone. Don't force it: a handoff covering M0 is not "issue 12's handoff".
- **`<type>`** — `handoff`, `brief`, `plan`, `spec`, `research`.
- **`<slug>`** — short, kebab-case, English.

Current:

| File | Type |
|---|---|
| `handoffs/001-handoff-project-foundation.md` | Founding handoff — **the source of truth**, as amended by the table at its top |
| `design/002-brief-design-direction.md` | Visual direction brief, feeds the Claude Design exploration |
| `research/003-research-universal-rn-web.md` | Whether universal Expo Router + react-native-web fits this product |
| `research/004-research-web-native-code-sharing.md` | Two UIs sharing a core, and the universal styling systems |
| `research/005-research-miolos-name-check.md` | "Miolos" domain + INPI check (2026-07-30 snapshot); sources the Android-share figure |
| `design/006-handoff-design-winner-atelie/` | Handoff bundle of the winning design direction (F "Ateliê"): six `.dc.html` reference frames + `support.js` + token sheet. Snapshot — the living files are root `PRODUCT.md`, `DESIGN.md` and `packages/ui/tokens.css` |
| `plans/007-issue-14-plan-monorepo-foundation.md` | Implementation plan for #14 (monorepo foundation) |
| `plans/008-issue-41-plan-security-hardening.md` | Implementation plan for #41 (CI and security-header hardening) |
| `plans/009-issue-15-plan-anonymous-identity.md` | Implementation plan for #15 (anonymous identity: users/sessions schema, `POST /session`, web bootstrap) |
| `plans/010-issue-16-plan-binairo-engine.md` | Implementation plan for #16 (Binairo engine) |
| `plans/011-issue-22-plan-sudoku-engine.md` | Implementation plan for #22 (Sudoku engine) |
| `plans/012-issue-24-plan-nonogram-engine.md` | Implementation plan for #24 (Nonogram engine) |
| `plans/013-issue-26-plan-termo-engine.md` | Implementation plan for #26 (Termo engine and word-list harness) |
| `plans/014-issue-17-plan-publication-pipeline.md` | Implementation plan for #17 (publication pipeline: buffer, cron, published-predicate wall) |
| `plans/015-issue-39-plan-impeccable-ci.md` | Implementation plan for #39 (impeccable detect against Vercel preview deployments in CI) |
| `plans/016-issue-43-plan-dependabot.md` | Implementation plan for #43 (Dependabot for SHA-pinned actions) |
| `plans/017-issue-18-plan-play-the-daily-binairo.md` | Implementation plan for #18 (play the daily Binairo: play and conclusion screens, write-once completions, dormant hint grants, the apps/web db wall) |
| `plans/018-issue-23-plan-daily-sudoku-end-to-end.md` | Implementation plan for #23 (daily Sudoku end to end: the shared `apps/web/src/play/` layer, the Sudoku buffer and screen, device-local day state and conclusion chaining) |
| `handoffs/019-handoff-m2-nonogram-and-termo.md` | Session handoff for M2's remaining two games — #25 (daily Nonogram) and #27 (daily Termo): the shared play layer's real API and its honest limits, every extension point, and the decisions each game must make |
| `plans/020-issue-25-plan-daily-nonogram-end-to-end.md` | Implementation plan for #25 (daily Nonogram end to end: the Nonogram buffer and its contracts, the ruled variable-size board and its three-state brush, the picture reveal in the conclusion, and completion, hint, timer and offline sync at parity) |
| `handoffs/021-handoff-m2-termo-and-free-play.md` | Session handoff for M2's last two — #27 (daily Termo) and #28 (free play): what #25 changed in the shared play layer, where handoff 019 is now wrong, and Termo's two unresolved structural decisions (it cannot play offline, and ADR-0027's client-side hint argument does not transfer) |
| `plans/022-issue-27-plan-daily-termo-end-to-end.md` | Implementation plan for #27 (daily Termo end to end: the answer draw and its no-repeat rule, the stateless guess route and the offline degradation, the tile board and the pt-BR keyboard, the conclusion's fourth state, and the accent rule that closes #68) |
| `handoffs/023-issue-27-handoff-termo-mid-flight.md` | Session handoff taken mid-#27 with PR #77 open and steps 1–5 complete: what the ten commits landed, the three facts that cost that session real time (drizzle's insert column list, the `@miolos/core` ESM cycle vitest cannot see, zod's mutable tuple), the already-applied migration, and the known-deferred list step 6 must be shown |
| `handoffs/024-handoff-m2-free-play-the-last-ticket.md` | Session handoff after #27 merged and the daily Termo went live: what the review changed, the four seams #28 (free play) already has, the silent-green traps in `bundle-check` and `impeccable detect`, and the obligations #27 transferred rather than discharged |
| `plans/025-issue-28-plan-free-play-on-the-grid-games.md` | Implementation plan for #28 (free play on the grid games: the `/modo-livre` routes and level picker over the weekday ramps, browser-side generation with client-picked seeds, the free-play import wall and the evidence plan for the three negative acceptance criteria, and the route-scoped rework of the bundle markers) |
| `handoffs/026-handoff-m2-complete-the-frontier-opens.md` | Session handoff after #28 merged and free play went live, closing M2: the new machinery that must not break (route-scoped fail-closed bundle markers, the free-play wall's hoist-don't-copy rule, ephemeral-by-decision free play), the re-derived test-id frontier, and the open frontier with #58's pending decision flagged for whoever starts M3 |
| `plans/027-issue-19-plan-hoje-hub-streak-and-pwa-manifest.md` | Implementation plan for #19 (Hoje hub, streak, and PWA manifest: the `computeStreak` pure function and its alive-until-the-rollover anchor, the first authenticated read `GET /streak` and its client-fetched delivery to the hub stamp and the conclusion card, the free-play wall extension over the streak modules, and the manifest, icons and install metadata — with the day-truth payload deferral recorded as proposed ADR-0048) |
| `handoffs/028-handoff-19-merged-the-streak-is-live.md` | Session handoff after #19 merged and the streak, the first authenticated read and the PWA manifest went live: the authenticated-read conventions `GET /streak` sets, the reciprocal `Amended by:` rule and the other machinery #19 leaves armed, why M1 still has one ticket (#20, now unblocked), and #58's decided-but-unimplemented status the next streak-adjacent plan must respect |
| `plans/029-issue-20-plan-account-merge-recompute.md` | Implementation plan for #20 (account-merge recompute: the pure `mergeCompletions` union-earliest-dedupe in `packages/core` with its opaque ordering key, the idempotent no-transaction `mergeAccounts` operation on `@miolos/db/user` — sessions remapped never deleted, the loser emptied and retained as a tombstone, no migration — the deterministic winner rule, the lost-Termo collision carve-out, and the semantics recorded as proposed ADR-0049) |
| `handoffs/030-handoff-20-merged-the-account-merge-lands.md` | Session handoff after #20 merged: the two-seam merge machinery and its tripwires (the T-DB-S22 agreement bridge, the T-DB-S24 column-list tripwire, the strict-< tie rule), ADR-0049's amendment of ADR-0026, the #21 security preconditions recorded on the issue, the correction that M1 completes with #21 not #20, and the re-derived test-id frontier |
| `plans/031-issue-21-plan-email-attach-magic-link-merge-lgpd.md` | Implementation plan for #21 (email attach: the one request/confirm magic-link flow that serves attach, recovery, device move and merge-on-collision, the `attach_tokens` table and migration 0004 with the verified-email partial unique index, the winner-liveness guard discharging the merge-serialization precondition, timestamp-only consents kept per ADR-0022's conditional, the server-owned exactly-once prompt over a new `GET /attach/state`, Resend-via-fetch fail-closed transport, the `/privacidade` page with real self-service deletion, and the semantics recorded as proposed ADR-0050) |
| `handoffs/032-handoff-21-merged-m1-complete.md` | Session handoff after #21 merged, completing M1: the dormant-until-keyed attach stack and its security posture (cross-account session revocation closing the review-found takeover, the four recorded residuals), ADR-0050's flow-level amendment of ADR-0009, the preview-shares-prod-DB migration push-ordering rule, Fernando's activation checklist, and the re-derived test-id frontier |
| `plans/033-issue-29-plan-stats-calendar-dia-perfeito.md` | Implementation plan for #29 (stats, calendar, and Dia Perfeito: the pure derivations over one unfiltered reader — Dia Perfeito, the clamped account-birth calendar, the guess distribution with its unqualified fail row, the three-game times — the two closed contracts `GET /stats` and `GET /stats/calendar`, the `epochDay` hoist with its guarded inverse, the hub Termo tile's `em 4/6`, the conclusion stat block behind the recorded gate, the `/estatisticas` screen, and the semantics recorded as proposed ADR-0051) |
| `adr/0051-statistics-are-read-time-derivations-on-closed-contracts.md` | ADR-0051 (statistics are read-time derivations on closed contracts: no stored aggregate and no schema change, the account-birth calendar anchor with missed-by-absence and the clamped range edge plus its named #31 revisit, the two-endpoint split with the calendar payload's revisit trigger, three-game times with Termo's distribution, the `epochDay` hoist, and the three stat-row populations with their label obligation) |
| `handoffs/034-handoff-29-merged-m3-opens.md` | Session handoff after #29 merged, opening M3: the stats stack live in production (pure derivations on closed contracts, the won-only calendar clamp closing the review-found pre-birth-missed bug, the CI-caught mobile layout fix), the new stats read conventions and midnight-boundary rule, the `TURBO_CONCURRENCY=1` pre-commit landmine, the recorded residuals #30/#31/#37 inherit, and the re-derived test-id frontier |
| `plans/035-issue-30-plan-medals.md` | Implementation plan for #30 (medals: the 23 AI-curated definitions authored against `content/medals/README.md`'s written constraints with the ≥ 10-row rejected sample, the pure `earnedMedals` derivation over the unfiltered stats reader with the closed `MedalRule` union, the `medal_grants` table with its operator-ritual write path and the one-statement `least()` union-earliest-dedupe on merge, the shape-validated `GET /medals` id-set contract with the drop-unknown client rule, the nothing-at-zero row-list display on `/estatisticas`, the AC 3 tripwire triad, and the semantics recorded as proposed ADR-0052) |
| `adr/0052-medals-are-derived-facts-plus-curated-grants.md` | ADR-0052 (medals are derived facts plus curated grants: the two medal kinds and the curated-grant definition, recompute-never-store with the stored-rule-derived-id-ignored rule, the closed rule-input list, `medal_grants`' shape with the operator write path and the earliest-wins merge, the earned-id-set-only contract with shape validation and the `earnedDate` omission handed forward, the no-wallet tripwires with their named residual, ADR-0015 scaled to a hand-sized catalog, the ladder defence, the founder medal's #37 launch obligation, and the ×3 read-amplification restatement) |
| `handoffs/036-handoff-30-merged-medals-live.md` | Session handoff after #30 merged: 23 medals live on /estatisticas and GET /medals, the grants table applied pre-push, ADR-0052's amendment of ADR-0018, the VoiceOver and CLS-attribution review catches, the plan-register discipline rule, Fernando's four open flags, and the re-derived test-id frontier |
| `plans/037-issue-31-plan-archive.md` | Implementation plan for #31 (archive of past dailies, shipped as two pull requests: the write window's lower bound removed and replaced by a per-user-per-SP-day ceiling with the constant split into `ROLLOVER_SLACK_DAYS` and `isWritableDate`, then the `/arquivo` index, month, day and four literal play routes over three new wall readers and a DB-clock date classifier sharing one spelling of the publication conjuncts, `force-dynamic` for the kill switch with no index and no migration, the late result rendered in place with bounded local retention, pre-birth late completions off-calendar, the date-bearing CI URLs discovered from the app, and the semantics recorded as proposed ADR-0053) |
| `adr/0053-the-archive-is-a-public-past-only-read-and-a-late-write.md` | ADR-0053 (the archive is a public, past-only read and a late write: the four route families with today's date redirecting and the read-then-classify ordering, `force-dynamic` on the kill switch with `revalidatePath`-before-cache as the standing precondition, the data as its own floor, one spelling of the wall's conjuncts and of the SP clock with no index and a revalidate-first ordering rule, the write window's lower bound removed with what forgery actually buys accepted on ADR-0006's terms, the constant split one-owner-each, pre-birth late completions off-calendar, the unqualified fail row with its AC carve-out, no archive conclusion route, "you solved this" discharged at the write path, Termo included, gate URLs discovered not hardcoded, the chosen 50-late-writes-per-SP-day ceiling enforced inside the insert, which reverses ADR-0026's rejected rate limit for that branch, bounded play-record retention, and the two-PR deploy ordering as a rule — amending ADR-0008, 0014, 0024, 0026, 0028, 0038, 0039 and 0051) |
| `handoffs/038-handoff-31-merged-the-archive-is-live.md` | Session handoff after #31 merged as two sequenced pull requests and the archive went live: the public past-only read and the late write with its in-INSERT ceiling, the write window's split into `ROLLOVER_SLACK_DAYS` and `isWritableDate`/`isLateDate`, ADR-0053's ten reciprocal amendment lines across eight records, the four catches worth keeping as pattern (the probe that verified nothing, the check-then-act ceiling, the 429 that would have cost a streak day, and the two calendar bombs no commit could reach), the new landmine that a data- or copy-dependent gate goes red on the calendar, #96 filed and #34 unblocked, Fernando's eight flags, and the re-derived test-id frontier |
| `handoffs/039-handoff-post-31-the-gate-moved.md` | Delta session handoff over 038, covering the four PRs that landed after #31 without touching product code (#80, #98, #99, #100): CI made `pull_request`-only under a per-ref concurrency group so "CI green on main" is gone as evidence, the Vercel remote-cache token replaced by a branch-scoped `actions/cache` whose first run per PR is cold by design, the seven-day `minimumReleaseAge` and `trustPolicy: no-downgrade` that let `pnpm install` hard-fail on purpose with `minimumReleaseAgeStrict`'s inherited-vs-declared default as the trap, #31's eight flags recorded as confirmed in ADR-0053, the two Dependabot security settings enabled outside git, and what #34 inherits |
| `plans/040-issue-34-plan-sharing.md` | Implementation plan for #34 (sharing: the spoiler-free share text composed on the client from the local play record — coloured squares as content in a channel with no CSS, Termo's grid as the one game with a shape, and the answer, the streak, the medals and any claim about *when* the day was solved all excluded; one string through `navigator.share` with a clipboard fallback, falling through to the clipboard on any non-`AbortError` rejection; eight dated, `force-dynamic` Open Graph routes plus one committed static asset for the site card, over one card kind that reads the wall as an existence proof and draws no puzzle content — the daily four included, because the redirect-following scraper majority reads the daily route's head on the day people share; one wall read each, `getPublishedDaily` by date and `getTodayDaily` for today, so there is no midnight race; `openGraph` and nothing else on the daily routes, leaving their crawl-facing metadata byte-unchanged; committed static font instances at the app's own weight **and at the optical size the card is READ at rather than the size it is authored at**, because satori cannot read woff2, throws on a variable font, and synthesises no optical size; the standing OG lint wall written out so it cannot silently delete the db wall it overlaps; a database outage answering 500 while a bad row answers 404; and the semantics recorded as proposed ADR-0054, which grows **four** per-route enumerations — including the `revalidatePath` path list a future `killed_at` writer will be built from, and ADR-0039's fifth-game route recipe, which three consecutive audit passes cleared as untouched) |
| `adr/0054-the-share-is-plain-text-and-the-card-is-a-nameplate.md` | ADR-0054 (the share is plain text and the card is a nameplate: the composer inside the conclusion with no fifth prop, coloured squares as content in a channel with no CSS and no shape invented for the three games that have none, the exhaustive in/out list that keeps the answer, the streak, the medals, the hint count and any claim about *when* out of the share, one string through `navigator.share` falling through to the clipboard on every non-`AbortError` rejection, the per-day permalink as the share URL; eight dated OG routes plus a committed static site card, over one card kind — eight dated and `force-dynamic` because the redirect-following scraper majority reads the daily head — one wall read used as an existence proof with the card's date as its only field, a bad row 404ing while an outage still 500s, `openGraph` and nothing else on the daily routes so their crawl-facing metadata stays byte-unchanged, `OG_DEFAULTS` spread into every leaf because Next replaces rather than merges, three committed static font instances at the app's weight and at the optical size the card is READ at with the criterion that chose it, and the standing OG lint wall that repeats the db wall it overlaps — amending ADR-0028, ADR-0053, ADR-0039 and ADR-0043, with annotations on ADR-0045, ADR-0002 and ADR-0038) |
| `handoffs/041-handoff-34-merged-sharing-is-live.md` | Session handoff after #34 merged and sharing went live: the spoiler-free share text with Termo's coloured-square grid as the one game with a shape, eight dated `force-dynamic` Open Graph routes plus a committed static site card, and the metadata that lets a redirect-following scraper read a per-day image on the day people share. Carries what the eight steps cost — three plan-review rounds, six diff lenses, a two-pass fix and a verification round — and the four measured mechanics they turned up: a root-segment metadata module inflating every descendant's serverless trace, leaf `openGraph` replacing rather than merging, satori refusing px stops in a tiled gradient, and `css2` quantising `opsz` into buckets it serves silently. Plus the seven-times-repeated pattern of a record falsified by the work that cleared it, the two-way amendment check that finally caught it, and the root-`pnpm test` flake filed as #107 |
| `plans/043-issue-96-plan-archive-done-chip.md` | Implementation plan for #96 (the archive day page's per-game done chip: the repair of `useRecordSnapshot`'s single-slot cache into a bounded `(game, date)`-keyed `Map` — the mechanism the issue names, broken for the shape the issue asks for, and broken on the date axis too now that the date is a URL variable; the whole day card as the client boundary, because the anchor's `aria-label` replaces its content so the label and the chip must move together; an in-flow flex row beside the kicker rather than an absolute corner stamp, at a measured permanent cost of 8px per card on a dial that trades growth against the tape's clearance; `Feito` / `Jogado` — two verbs, because a lost Termo showing *Feito* is a false done — under the hub's on-time word for a late completion, with distinct done and played accessible names; no time, no result figure and no server read; and the semantics recorded as proposed ADR-0056) |
| `plans/046-issue-96-research-review-round-1-findings.md` | Round-1 plan-review findings for #96 — five lenses, 5 × REJECT. A point-in-time record, never edited, not even to mark a finding fixed |
| `plans/047-issue-96-research-review-round-2-findings.md` | Round-2 plan-review findings for #96 — three lenses, 3 × REJECT. Same rule |
| `adr/0056-the-record-snapshot-cache-is-per-key-and-the-done-chip-wears-the-hub-word.md` | ADR-0056 (the record-snapshot cache is per key, and the archive's done chip wears the hub's on-time word: the single slot's cross-game AND cross-date loop, the count bound deleted after step 6 measured bound + 1 looping the same way, staleness swept off the poll as the only collector, and the two siblings deliberately not repaired; the `Conclusão tardia` / `Feito` collapse taken for cross-surface consistency and reversible in one line, with the two verbs not reversible and the stats-calendar counter-precedent engaged as record-versus-affordance; the done card's accessible name keeping a verb and the done and played names differing because the `aria-hidden` chip leaves the label as assistive tech's only channel; and every accent ring in a shared sheet enumerated by a scan rather than by a list of instances) |

## Language

English for filenames, new documents, code and commits. pt-BR for user-facing product content, and for the two founding documents above, whose **bodies** stay as written — corrections are appended as an amendment table, never edited inline.
