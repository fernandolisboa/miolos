# Evidence — #35 onboarding card (PR #150)

Step-8 evidence for a feature invisible on preview deploys (ADR-0048: the credentialed call is anonymous on every `*.vercel.app` origin, so no preview can render the card). Static-fixture screenshots at 390×844 and 1440×900, with-card / without-card (the dismissed state), embedded in PR #150's body.
